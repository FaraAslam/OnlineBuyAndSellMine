export interface LoginModel {
    userId: string;
    userType: string;
    userEmail: string;
}

export interface SignUpModel
{
    email:string,
    password:string,
    firstName: string,
    lastName: string
}